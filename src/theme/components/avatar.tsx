import {
  Avatar as OAvatar,
  AvatarGroup as OAvatarGroup,
  AvatarIcon as OAvatarIcon,
  extendVariants,
} from "@heroui/react";

export const Avatar = extendVariants(OAvatar, {
  defaultVariants: {},
});

export const AvatarGroup = extendVariants(OAvatarGroup, {
  defaultVariants: {},
});

export const AvatarIcon = extendVariants(OAvatarIcon, {
  defaultVariants: {},
});
