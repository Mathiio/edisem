import { Checkbox as OCheckbox, CheckboxGroup as OCheckboxGroup, extendVariants } from "@heroui/react";

export const Checkbox = extendVariants(OCheckbox, {
  defaultVariants: {},
});

export const CheckboxGroup = extendVariants(OCheckboxGroup, {
  defaultVariants: {},
});
