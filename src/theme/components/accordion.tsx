import { Accordion as OAccordion, AccordionItem as OAccordionItem, extendVariants } from "@heroui/react";

export const Accordion = extendVariants(OAccordion, {
  defaultVariants: {},
});

export const AccordionItem = extendVariants(OAccordionItem, {
  defaultVariants: {},
});
